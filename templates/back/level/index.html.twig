{% extends 'base.html.twig' %}

{% block title %}Level index{% endblock %}

{% block body %}
{% include 'back/partial/back_nav_bar.html.twig' %}
    <h1>Level index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for level in levels %}
            <tr>
                <td>{{ level.id }}</td>
                <td>{{ level.name }}</td>
                <td>
                    <a href="{{ path('app_back_level_show', {'id': level.id}) }}">show</a>
                    <a href="{{ path('app_back_level_edit', {'id': level.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_back_level_new') }}">Create new</a>
{% endblock %}
