{% extends 'base.html.twig' %}

{% block title %}Level{% endblock %}

{% block body %}
{% include 'back/partial/back_nav_bar.html.twig' %}
    <h1>Level</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ level.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ level.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_level_index') }}">back to list</a>

    <a href="{{ path('app_back_level_edit', {'id': level.id}) }}">edit</a>

    {{ include('back/level/_delete_form.html.twig') }}
{% endblock %}
