{% extends 'base.html.twig' %}

{% block title %}QuestionAnswer{% endblock %}

{% block body %}
{% include 'back/partial/back_nav_bar.html.twig' %}
    <h1>QuestionAnswer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question_answer.id }}</td>
            </tr>
            <tr>
                <th>IsGood</th>
                <td>{{ question_answer.isGood ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_question_answer_index') }}">back to list</a>

    <a href="{{ path('app_back_question_answer_edit', {'id': question_answer.id}) }}">edit</a>

    {{ include('back/question_answer/_delete_form.html.twig') }}
{% endblock %}
